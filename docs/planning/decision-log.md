# Planning decision log

Recorded 2026-09-04 from the project owner's instructions. Documentation/planning only; no implementation or new runtime validation. Companion documents: [landscape and retained source evidence](../research/zakura-common-landscape.md) and [API validation workplan](api-surface-workplan.md).

**Decided** records a settled owner constraint. **Direction** records a preferred design approach whose details need review. **Deferred** excludes work from current delivery while preserving a reconsideration path. **Open** requires a follow-up decision or validation. Recommendations below do not settle Open items. Except for the explicitly approved namespace names in D18/D21, every proposed public package, module, type, method and operation name remains **PRELIMINARY**; existing third-party names are evidence only.

| ID | Classification | Decision or disposition | Consequence / next evidence |
| --- | --- | --- | --- |
| D01 | Decided | Forking Zakura and adding a new WASM binding layer is acceptable. Prefer a new wrapper crate/workspace member depending on Zakura crates. Avoid changing existing packages unless target compatibility requires a minimal upstreamable change. | A feature flag alone does not create JS bindings. Put exports, conversions, ownership and host integration in the wrapper; use preliminary wrapper features such as `transparent`, `sapling`, `ironwood` and `proving`. Add dependency-crate features/fixes only when compilation requires them, with a reproducible failing target probe and minimal patch rationale. Validate one coherent Common/wallet graph. |
| D02 | Decided | Deliver a fully fledged, clean Node.js npm product without a messy user-facing split. | Required in-scope functionality must have a supported Node path; users should not assemble incompatible binding packages or rewrite application flows per backend. This does not itself require a native addon. |
| D03 | Direction | Recommend one stable TypeScript API with runtime-selected internal backends. | Keep contract semantics, errors, authority and lifecycle consistent across supported backends. Public names and packaging remain preliminary. WASM-first delivery and deferred acceleration follow D07 and D03a below. |
| D03a | Deferred | Start WASM-first on Node and browser per the owner’s D07 refinement; no current N-API promise. | Native/N-API acceleration requires a measured WASM budget shortfall and a separate decision through the [tracked benchmark issue #11](future-issues.md#issue-11-wasm-performance-and-possible-native-acceleration). No benchmark or native probe is current work. |
| D04 | Decided | Primary protocol scope is transparent + Sapling + Ironwood. | Give Sapling and Ironwood distinct capability rows. Existing Orchard source names and evidence are not an Ironwood capability or activation guarantee. Sapling proving-parameter acquisition, integrity checks, caching, packaging and worker memory are now v1 requirements. |
| D05 | Deferred | Sprout support is explicitly deferred and backtrackable. | Track Sprout decoding, scanning, spending and migration separately; it is not a readiness requirement. Revisit only for a concrete recovery or compatibility requirement. |
| D06 | Open | Exact Zakura Common/wallet Sapling and Ironwood API coverage and complete transaction/proving flows. | The [symbol-level capability map](../research/zakura-api-capability-map.md) traces explicit Ironwood V3/transaction-v6 paths through scanning, SQLite, selection, building, PCZT authorization/proving and extraction, with shared Orchard keys/receivers and internal `orchard` gates distinguished from legacy Orchard. Source tests show conditional sign-before-witness/proof ordering; the standard proposal-to-PCZT API rejects multi-step plans. The map’s Sapling appendix adds source symbols and parameter/authority requirements without runtime validation. End-to-end coverage on the selected graph, explicit network context, transport/confirmation/reorg and host persistence remains unverified; do not mark decided or infer activation/device support. |
| D06a | Decided | Use the wallet libraries' coherent, locked Zakura Common `1.0.0` dependency graph as the initial baseline. Track migration of the complete graph to Common `1.1.0` as a follow-up issue. | Do not mix individual `1.1.0` crates into the pinned `1.0.0` graph. The upgrade issue must align every relevant edge, run the graph-audit script, compile supported targets and execute transparent + Sapling + Ironwood flow tests before changing the baseline. |
| D07 | Decided* (owner refinement) | WASM-first Node and browser, with bundled SQLite inside the wallet WASM module: Node filesystem VFS and browser OPFS VFS, each in a dedicated worker. This supersedes earlier `native SQLite on Node` wording. | One TS API; non-shared baseline plus separate threaded shared-memory artifact. Node worker_threads from baseline; qualify Node Rayon bootstrap and isolated-browser SAB fast path, with baseline fallback. One owner serializes SQLite; crypto may parallelize. Prior mixed-graph compilation and failed final SQLite link are not runtime proof. Scanner serial/threaded liveness and both VFS durability paths require [functional gates](wasm-host-architecture.md#functional-acceptance-gates). Native/N-API remains future benchmark-triggered work. |
| D08 | Decided | Expose independently constructible public, light and wallet clients, plus an optional composed Zcash client that presents them as `zcash.public`, `zcash.light` and `zcash.wallet`. Public clients use HTTP JSON-RPC transports; standard light clients use `grpc(url)`, with browser gRPC-Web mechanics hidden internally where the endpoint permits. The library will not operate or require project-owned infrastructure. | Keep public JSON-RPC, low-level lightwallet gRPC and stateful wallet responsibilities distinct. Allow callers to inject or use each client independently; the composed client shares them without duplicating state. Applications or external provider presets supply endpoints. Custom light transports remain possible without weakening the standard gRPC path. Validate protocol/network on first use and preserve privacy and unknown-submission semantics. |
| D09 | Direction | Expose manual lifecycle stages and ergonomic composed operations, informed by the [viem, ethers and WebZjs API-shape review](../research/transaction-api-inspiration-review.md). | Prefer viem-style factories and named arguments, explicit proposal/artifact stages, and a Zcash-specific pending payment handle with `wait` over a bare hash or provider-bound response. Public raw broadcast remains wallet-independent and returns a plain per-attempt report. Automatic and manual paths share the same proposal engine. Exact names and durable journal semantics still need owner review; ordering follows the mapped Zakura/PCZT roles. |
| D10 | Decided | WebZjs was made by someone involved with this project (owner context). Use it as a requirements/prior-art guide, not a complete capability map or compatibility target. | No snapshot/API compatibility requirement, exhaustive compatibility map, adapter or migration promise. Derive independent requirements and validate selected protocol formats on their own merits. Retain the cited inventory as evidence. |
| D11 | Decided | The generic external signer/PCZT seam remains in v1. Concrete Ledger support is only a documented stub/placeholder and is unsupported until later device, app/firmware, pool, version and display/review qualification. | Separate signer authority from orchestration and proving; support reviewable handoffs, capabilities and resumable roles. Validate specific devices/firmware, pools, versions, display/review and returned authorization. The owner context is not an independently verified device-support matrix or an Ironwood support claim. |
| D12 | Deferred | Remote proving is separate from external signing and deferred to avoid scope creep. | No remote service, protocol, deployment or witness-transfer workflow in current delivery. Revisit for a concrete resource/use-case need after local proving measurements and explicit privacy/trust review. |
| D13 | Direction | The prover interface must not block future remote proving. | Describe jobs/results by protocol/circuit/version and required proving authority; keep scheduling and transport outside core proof semantics. Avoid requiring same-process pointers or access to signing secrets. Future serialization and witness disclosure require their own design; never silently fall back to a remote prover. |
| D14 | Decided | Separate wallet account records, secret custody and signer authority behind a small JS adapter that mirrors Zakura’s actual paths. | A wallet account maps to Zakura `Account`/opaque `AccountId`; tracking maps to `AccountPurpose`; a local mnemonic signer retains an opaque USK and supplies `SpendingKeys` to `create_proposed_transactions`; an external/custom signer uses `create_pczt_from_proposal` and PCZT signer roles. The common JS signer surface must not force the local path through PCZT because Zakura’s direct local path supports multi-step proposals while proposal-to-PCZT is currently single-step. A DB account UUID, ZIP32 account index and receive address remain distinct identities. |
| D15 | Direction | Treat viewing capability upgrades, signer attachment and spending-state recovery as separate transitions. | The inspected SQLite `add_account`/`upgrade_account_ufvk` path accepts strictly subsuming viewing material but does not change spending-purpose flags or reconstruct historical state; identical imports collide. No public in-place purpose-upgrade operation was established. Retaining spend data permits later matching signer attachment; true view-only upgrade delivery remains unresolved. See [upgrade cases](keys-accounts-signers-api.md#viewing-only-attachment-and-upgrades). |
| D16 | Decided | V1 excludes all raw spending-key export, including Sapling, transparent, Orchard/Ironwood and USK material. | No keyring `exportSecret`, reveal-bytes API, export permit or one-time backup exception in v1. Ordinary wallet serialization and SQLite never contain spending secrets. Applications retain their own mnemonic backup; viewing export remains a separate explicit disclosure. Wallet-database confidentiality remains a separate question. |
| D16a | Deferred | Do not build encrypted persistent spending-key or mnemonic custody into v1. Applications retain the mnemonic or provide an in-memory/external signer when authorization is required. | Keep spending secrets out of Zakura SQLite and ordinary wallet backups. A future encrypted key vault is a separate feature with its own format, unlock, recovery and security review; signer injection must allow adding it later without changing wallet/account APIs. Wallet-database confidentiality remains separate; D16 settles v1 secret-export exclusion. |
| D17 | Decided | Mirror Zakura's account lifecycle instead of maintaining a separate JavaScript account-index ledger. | `wallet.accounts.create({ mnemonic })` wraps `WalletWrite::create_account`: Zakura transactionally selects the next sequential ZIP-32 index from the stored seed fingerprint and returns the account ID plus USK-backed memory signer. `wallet.accounts.import({ mnemonic, accountIndex, birthday })` wraps `import_account_hd`; UFVK import wraps `import_account_ufvk`. `accounts.create({ mnemonic })` has no birthday input: after explicit sync it derives `AccountBirthday` internally from one coherent current locally verified wallet database chain/tree snapshot. No hidden network request or implicit sync occurs; unavailable/stale local state fails `SYNC_REQUIRED` before mutation. See [creation semantics](../api/accounts-signers.md#choose-the-onboarding-path). Recovery requires an explicit birthday or full scan. The database stores derivation metadata and viewing authority, never the mnemonic. Namespace spellings are approved by D21 and governed by the authoritative [namespace audit](api-namespace-audit.md). |
| D18 | Decided | Use resource-oriented wallet receive-address names: `wallet.addresses.current/next/list/at`, directly mapping to Zakura `get_last_generated_address_matching/get_next_available_address/list_addresses/get_address_for_index`, respectively. | `current` and `list` read existing records; `next` and `at` generate, persist and mark exposure. No implicit allocation on a current-address miss, no search-forward behavior for `at`, and no nested per-account address service. Preserve receiver and transparent recovery-range constraints; standalone derivation remains separate. See the [namespace audit](api-namespace-audit.md) for the approved namespace tree (D21). |
| D19 | Decided | zcash.js will not provide mnemonic or seed generation. | Applications use a reputable BIP39 npm package and pass the resulting mnemonic to `wallet.accounts.create({ mnemonic })` or `wallet.accounts.import({ mnemonic, accountIndex, birthday })`. Applications and their chosen package own generation entropy/CSPRNG and mnemonic backup; zcash.js validates supplied mnemonics and derives account authority. Runtime randomness needed for other cryptographic operations remains separate. |
| D20 | Deferred | Unstable and less-common key/account imports are outside v1; current wallet onboarding uses mnemonic or UFVK paths. | The [tracked future issue](future-issues.md) specifies UIVK-only wallets, transparent WIF/private-key and P2SH/redeem-script imports, raw Orchard/Ironwood component keys, unstable USK binary import/export, coherent ZeWIF import and assessment of any future stable Zakura mnemonic utility. It is tracked as [issue #10](https://github.com/jp4g/zcash.js/issues/10) in the post-v1 milestone; issue creation does not expand v1. |
| D21 | Decided | Approve `wallet.accounts.create/import/list/get/remove/attachSigner/detachSigner`, `wallet.pczt.export/import` and `wallet.operations.list/get/resume`, alongside D18 addresses. Keep wallet send/sync/balance/history and other ordinary orchestration/queries flat; PublicClient and LightClient stay flat. | The [namespace audit](api-namespace-audit.md) is authoritative. Operations implementation must still satisfy durable-state validation; naming approval is not runtime evidence. |
| D22 | Decided | UFVK import directly calls `WalletWrite::import_account_ufvk` with public `viewOnly?: boolean`, default `false`. Omitted/false maps internally to `AccountPurpose::Spending { derivation: None }` and retains spend-supporting state; it does not imply, store or manufacture a spending key or signer. `true` maps to `AccountPurpose::ViewOnly` and may require reconstruction/rescan before later spending. Zakura has no public `import_account_uivk`; UIVK-only account import is excluded from v1 and stays in the [tracked future issue](future-issues.md), without inventing a backend path. | Signer attachment, viewing upgrades and spending-state recovery remain separate; purpose is not secret possession. |
| D23 | Decided | The default address request maps exactly to Zakura’s `UnifiedAddressRequest::AllAvailableKeys`: include/require every available supported receiver on the account (transparent, Sapling and the Orchard-encoded receiver used for Ironwood). Publishing this UA links these receivers and permits transparent receipt. Shielded-only is an explicit supported request, never the default; missing required receivers fail without fallback. | Preserve explicit receiver requests, recovery-range constraints and privacy documentation. |
| D24 | Decided | Mnemonic recovery accepts every checksum-valid standard BIP39 word count: 12/15/18/21/24. Any preference for 24 words is documentation only, with no runtime warning or result field. zcash.js generates no mnemonics or seeds (D19). | Validate all five word counts plus checksum, wordlist, normalization and passphrase behavior; no shorter-phrase warning policy remains open. |
| D25 | Decided | Multisig and remote-wallet RPC are out of current scope and must not be designed now. | Do not add APIs, protocols or current delivery gates for them. Preserve D16/D19/D20 and the tracked future work. |

## WASM-first runtime and storage (D07 refinement; D03a deferred)

The owner's latest direction refines D07’s Node implementation detail while preserving Rust-owned Zakura SQLite and D04–D25’s other settled constraints. Both runtimes start with bundled SQLite in the wallet WASM instance; Node supplies a filesystem VFS and browsers an OPFS VFS. A separate native SQLite connection cannot be injected across unrelated WASM/native memories for free. N-API/native acceleration is a future measured-budget decision, not a current delivery promise.

The [host architecture](wasm-host-architecture.md) specifies the baseline/threaded artifacts, Node worker_threads/shared Rayon qualification, secure isolated-browser SAB selection, no-SAB scanner/VFS fallback, serialized database ownership, handle/buffer contracts and functional gates. Earlier host-owned IndexedDB alternatives and native-Node defaults are superseded planning alternatives, not active recommendations.

The asterisk marks implementation risk: the earlier spike compiled a mixed published graph, not the selected full Common 1.0.0 graph; its real SQLite link probe remained unresolved at `sqlite3_os_init`. No final wallet-module instantiation, scanner liveness or durable Node/browser VFS has been demonstrated. Require close/destroy/reopen and crash/migration/single-writer proofs before durability claims. If the exact pinned adapter proves impractical, revisit the adapter/backend with evidence while preserving the public API.

## Transaction/query integration under existing decisions

The [transaction/query plan](transaction-query-api.md) concretizes D09/D14/D21: ordinary `send({ accountId, to, amount }) → PendingPayment → wait()`, explicit account identity and attached or supplied signer; `send({ proposal })` executes exactly the reviewed plan. Local creation is fused and supports Zakura’s actual multi-step dependencies. Advanced build/prove/sign/finalize are single-step PCZT semantics, preserving approved `wallet.pczt` and `operations` namespaces.

Operation/outbox persistence, exact-byte retry, revisions, full inventory/history projections and complete enhancement-aware sync are new product glue over source primitives, not existing Zakura pending/query APIs. Use Rust-owned schema-pinned projections and valid extension transactions/external migrations. Public accounts use `viewOnly`; v1 Pool remains transparent/Sapling/Ironwood with explicit unsupported legacy accounting. These refinements preserve D04–D25 except the expressly updated D07 runtime choice; names/signatures outside settled namespaces remain planning candidates.

## Transport architecture (D08 — Decided)

Use the retained [network-interface evidence](../research/zakura-common-landscape.md#network-interfaces-that-must-be-validated-individually). These choices concern orchestration and deployment, not an assumption that indexers, node RPCs and wallet daemons expose interchangeable methods.

| Option | Benefits | Costs / validation needed |
| --- | --- | --- |
| Host-side adapters: lightwallet protocol for sync, explicit node JSON-RPC for applicable public queries; Node gRPC and browser gRPC-Web/gateway implementations | Fits one TypeScript API while keeping endpoint policy, credentials, cancellation and streaming outside crypto. Recommended starting direction. | More than one wire adapter; validate protobuf revision, deployed methods, Sapling and Ironwood scan/transaction data, browser streaming/proxy/CORS and per-method trust semantics. A gateway is a real deployment requirement, not automatically available. |
| Rust-owned networking reused from wallet/backend stacks | Could reuse existing orchestration and codecs where compatible. | Couples runtime/network authority to wallet engine; target-specific async runtimes and browser hosting need probes. Makes host customization and privacy policy harder unless a clear injection boundary exists. |
| Required application gateway with one custom HTTP/streaming contract | Can simplify client deployment interfaces and centralize protocol translation/authentication. | Introduces server operations, availability, schema maintenance and another observer of queries. Risks requiring infrastructure for ordinary Node use. Keep as an optional adapter unless an evidenced product requirement justifies making it mandatory. |

Use protocol-typed host adapters and lazy network-only public queries. Public clients use `http(url)` for the supported node JSON-RPC contract. Standard light clients use `grpc(url)` for the protobuf lightwallet contract; the adapter selects native gRPC on Node and browser-compatible gRPC-Web mechanics where supported without exposing separate constructors. Validate the selected endpoint with one harmless protocol handshake when the client first connects, rather than requiring callers to declare method-by-method capabilities. Custom light transports may implement the same client transport interface. A composed Zcash client namespaces shared instances under `public`, `light` and `wallet`; each constituent client also remains independently constructible and injectable. Composition must enforce one network identity and must not duplicate injected client state or assume ownership of externally managed resources.

zcash.js will not operate or require its own node, lightwallet server or custom gateway. Applications supply endpoints directly or through optional external provider presets. Node gRPC, browser gRPC-Web or third-party gateway support are transport adapters, not a reason to collapse the protocol boundaries. A lightwallet handshake uses its standard server-info method; a node handshake uses a harmless identifying/chain-info RPC selected for the supported node contract. If the endpoint does not match, fail at connection with a protocol or network mismatch. Optional methods can still return a normalized method-not-supported error when called; do not expose a large boolean capability manifest as required configuration.

Settle endpoint/network identity, authentication, schema/version negotiation, supported pools/methods, TLS/CORS, deadlines, backpressure, reconnect/resume, pruning and malformed responses. Record disclosure per request, including transparent-address queries, viewing data and endpoint failover; switching servers can change privacy exposure and must follow explicit policy. Validate Sapling and Ironwood coverage separately rather than assuming existing compact schemas suffice.

Reads can have bounded retry policies; a timed-out broadcast must retain an unknown outcome and finalized transaction identity/bytes. Reconcile through observation before any policy-approved rebroadcast of the same bytes; never blindly construct a new spend. Acceptance, mining, confirmation depth, reorg and expiry remain distinct. Assign a transport/privacy reviewer at G0 and resolve D08 through G3–G5 evidence.

## Transaction and signer review contract (D09, D11 — Direction and Decided)

Specify a state model before signatures or names: intent/proposal, reviewable plan, protocol-valid construction/PCZT roles, proofs and authorization in permitted order, finalization, submission and observation. A plan may yield multiple transactions; model partial progress and per-transaction identity. Lower-level operations and composed operations must share the same invariants and reviewed-plan identity.

The proposed typed pending handle should retain identity and submission evidence, including an unknown outcome when the response is lost. Its proposed wait behavior needs confirmation policy, reorg/expiry results, timeout/cancellation, resumable observation and partial-payment semantics. Cancelling observation cannot undo a broadcast. Composed operations must surface disclosures and external signer review before affected actions, and must not turn an unknown submission into a definite failure or success.

External signer capabilities must describe supported roles, pools, transaction/circuit versions and required review data. PCZT interchange, redaction, commitment checks, proof/sign ordering and final extraction need end-to-end validation with exact Common/wallet APIs. Ledger/Vizor prior art motivates this seam now; concrete Ledger support is a documented unsupported placeholder pending later qualification. Remote proving remains deferred.

Open choices have no invented owners, deadlines or benchmark outcomes. The workplan assigns reviewers at G0 and records evidence and disposition at the relevant gates. A later scope change should update this log and the affected capability rows together.
