# Private dependency inventory

The [machine-readable inventory](https://github.com/jp4g/zcash.js/blob/main/docs/planning/dependency-inventory.jsonl) records 223 npm lock entries and all 258 packages from the accepted wallet producer's Cargo metadata. It includes host/build/dev and target-conditional packages; it is not a claim that all are shipped. Raw license expressions are preserved, with feature selections, available notice-file hashes and source provenance. No license choice, legal clearance or vulnerability/security audit is performed.

Inputs are SDK `29aee5560f17c9c36ab3957d5519cb294fc47223`'s npm lock (unchanged from `8f57675`) and native `bb74dace75d6f1ca3c43108cf46390db9469b5e4`, build receipt `635f8a8163fbd94c4c7ff0a2d6a47b5b3971b070b18475f7bfbe7f4428e4cdb7`. Cargo metadata and wasm target feature logs were checked against that receipt. Registry and existing native-policy notice files were hash-checked against its package inventories; both owned Zakura patch/upstream identities remain recorded. The final inventory was regenerated from that producer’s metadata; package versions, observed licenses/notices and feature sets remain unchanged. Its source/archive identities and wrapper manifest hash replace the prior `1cc8b42` inputs explicitly. This dependency evidence is separate from the passing SDK host matrix and does not close #115.

## Remaining inputs

- All 223 npm entries declare licenses. Local version-matched files were available for 198; 25 optional platform packages remain metadata-only. Five installed packages have no discovered top-level notice: `@docsearch/css`, `@docsearch/js`, `@docsearch/sidepanel-js`, `@iconify-json/simple-icons`, and `@rolldown/binding-linux-x64-gnu`. Local notice bytes were not independently checked against npm tarball integrity, and nested notices/header obligations are not fully classified.
- Cargo declares licenses for 257 packages; the owned wrapper has none selected. Sixteen package expressions use legacy slash syntax, retained verbatim rather than interpreted as an SPDX choice. Sixteen packages have no discovered named notice in the receipt, including the wrapper; exact identities are in `unresolved`. Absence of such a filename is not a finding that the package has no license.
- SQLite 3.50.2's amalgamation/header hashes are bound to `libsqlite3-sys` provenance. Its source copyright disclaimer is recorded separately from the Rust wrapper's expression. The owned C VFS adapter has no selected project license.
- WASI SDK 27.0 executable/library hashes and archive digest are retained, but the installed SDK and retained archive contain no named license/notice files. Corresponding WASI libc/compiler-runtime/SDK source revisions and notices remain unresolved. Recorded library inventory is broader than linkage: the wallet build explicitly links static `libc`.
- Rust toolchain copyright documents and license texts are retained as local supplemental evidence, alongside the compiler and target-library hashes. Per-linked-object attribution, other standalone codec capsule producer graphs, and canonical proving-parameter distribution terms still need coverage before whole-package closeout.

Metadata includes choices and licenses with distinct obligations (for example npm `MPL-2.0` and `CC-BY-4.0`, and Cargo alternatives). Nothing here selects an alternative or asserts that distributing notices alone satisfies it. Project license selection remains outside authorization.

The extraction used only existing files and Node standard libraries; its retained script path/hash is in the inventory. No dependency install, producer modification, build or network audit was run. This narrows the missing inventory identified in the [private delivery notes](private-release-notes.md); notice reconciliation and the explicitly listed inputs remain open.

## Subsequent distribution reconciliation

The historical missing-notice observations above are resolved for the retained artifacts by [MIT distribution closeout](mit-distribution.md). Root LICENSE and the shipped `licenses/` directory contain the selected project license and original third-party notices; the original observational inventory is retained unchanged.
