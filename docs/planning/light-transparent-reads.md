# Internal light transparent reads

`src/clients/light-transparent-reads.ts` composes the frozen argument/result shapes for `getAddressUtxos` and `getAddressBalance`. Its module exports are internal test/composition entrypoints only: no root exports, factory, Network, initialization, acquisition or wallet integration. Caller supplies accepted initialized transparent-address and lightwire instances, a CustomLightTransport, and the private native encoding family main/test/regtest. Family is not chain identity.

**Selected server not qualified:** lightwalletd `09593edbee4ee68d47e5a53f8ce1c83514c4e8e6` can suppress backend errors. These generic mechanics require an independently established failure-faithful source before a caller can treat replies as answered balance/inventory queries. Matching schema revision and sourceId do not establish that prerequisite. This slice does not complete the selected-server capability.

Every address is passed unchanged to the accepted native decoder; no JS encoding inference, normalization, UA extraction or TEX conversion. Input is an ordinary data record with a dense array of 1–100 tokens. Test/regtest shared encoding is retained. Response addresses must occur in the admitted request and decode in the same family. Duplicate returned outpoints are malformed.

Both calls are unary, once only. GetAddressUtxos explicitly sends start_height `"0"`, max_entries `1001`; at most 1000 entries succeed. Reaching the extra entry fails RESOURCE_LIMIT, rejecting observed truncation without implicit unlimited requests or speculative height pagination; completeness also requires the source prerequisite above. This local budget is an adapter policy, not a server maximum. Successful empty lists and zero balances remain success. Response byte limit is 4 MiB, matching the accepted codec. Owned transport signals are aborted and dependent listeners detached on every exit.

`value_zat` is the native lightwire decimal int64 DTO, admitted as 0 through 9223372036854775807 and converted directly to bigint. This read contract reports wire balances, not a consensus spendability or MAX_MONEY proof. Negative int32 indices fail; heights must fit uint32, including genuine height zero (no invented sentinel). Scripts are independently allocated. ReplyList carries no checked chain point: `tip: null` means unavailable metadata, not absent UTXOs. No confirmations or identity are fabricated.

Source basis:

- Frozen `docs/api/public-api.ts` PublicUtxo/PublicUtxos/LightClient and `docs/api/host-mapping.md` light method mapping; D06a/D08/D18/D21/D26 remain unchanged.
- Wallet a9142ee100b3a563b7d9ba7a8e94201d00ad8154, `librustzcash/zcash_client_backend/lightwallet-protocol/walletrpc/service.proto`, messages AddressList/Balance and lines 205–222: uint64 startHeight, positive maxEntries (zero unlimited), int32 index, int64 valueZat, uint64 height, no tip hash. Native codec uses snake_case JSON fields; schema SHA pair is embedded in the adapter.
- [Selected lightwalletd service.go](https://github.com/zcash/lightwalletd/blob/09593edbee4ee68d47e5a53f8ce1c83514c4e8e6/frontend/service.go): getAddressUtxos forwards bounds, applies max-entry cutoff, reverses decoded display txid bytes and builds ReplyList; GetTaddressBalance calls the backend balance helper. Errors delivered by the transport remain failures; this server does not reliably deliver backend errors. Byte reversal applies specifically to this method, not every protocol hash.
- Wallet `src/sync.rs:511–550` consumes the same UTXO reply through OutPoint and checked nonnegative values/uint32 heights. Its unbounded streaming request is not copied here.
- Accepted private transparent-address source at main 89659c80b38690f3f5a590c8e19b38fcc355b96b, packet-final receipt SHA256 a5b377630be1496949b17a463767c48af99821c343db8caa0c50aca513a5cc4a. Lightwire source 84585f25f7fed6a897891f5fb5bc509c76dddd05, receipt f0a385adffe4bdca50e39b951658f2d021ebb5011ceb00b6b78f63b988e3b5cc. Test artifacts are copied to owned scratch after source and artifact hash verification.

Node tests use both actual WASM instances, synthetic protobuf bytes and actual gRPC-Web transport with synthetic fetch. The browser runner follows the accepted finite Firefox lifecycle, with ordinary security and a local synthetic server. Local browser attempt failed with loopback EPERM before browser launch; no browser pass is claimed. Source/hash-bound parent command and detailed execution records are in `/home/jack/zcash-light-transparent-reads-logs/{REPORT,COMMANDS}.md`. Parent owns host verification and independent HIGH review. No live provider, native gRPC install, loader qualification, storage/VIEW clearance or full-client acceptance is implied.

## LTR-P1-1: exact upstream prerequisite

In pinned `frontend/service.go:929–939`, an unrecognized `getaddressutxos` backend error leaves `code` at zero. The pinned [go.mod](https://github.com/zcash/lightwalletd/blob/09593edbee4ee68d47e5a53f8ce1c83514c4e8e6/go.mod) selects grpc-go v1.82.1: [status.Errorf/Error](https://github.com/grpc/grpc-go/blob/v1.82.1/status/status.go) delegates to [Status.Err](https://github.com/grpc/grpc-go/blob/v1.82.1/internal/status/status.go), which returns nil for OK. The unary wrapper at service.go:984–996 then returns an empty ReplyList successfully. Its wire result is identical to genuine emptiness. The source-derived synthetic witness retains that empty message plus status 0 and asserts `items: []`, `tip: null`; it does not execute Go or inject an actual backend failure.

The balance helper has the same unset-code branch at service.go:611–621, returning `(nil, nil)` through GetTaddressBalance. Its eventual server marshaling behavior is unqualified here; the fixture does not claim this proves a false-zero balance. Genuine protobuf zero still succeeds, and delivered nonzero gRPC statuses fail for both methods.

The lightwalletd upstream maintainer must ensure every backend failure in both helpers returns a non-OK error, including unmatched errors, while preserving real empty/zero success. Upstream qualification needs backend-failure injection tests through both unary handlers and actual marshaling, including recognized and unmatched errors, cancellation and genuine empty/zero controls. The coordinator owns obtaining/reviewing an exact corrected source revision and qualifying its end-to-end transport before integrating a selected-server capability. No upstream change or new production profile is made here. SDK error-string parsing, retries, empty/zero rejection or invented metadata cannot recover the erased distinction.

R1 evidence and the hash-bound coordinator Firefox command are in `/home/jack/zcash-light-transparent-reads-logs/fixes/r1/{REPORT,COMMANDS}.md`. This correction preserves generic mechanics only; selected-server acceptance remains blocked on the upstream prerequisite and independent review.
