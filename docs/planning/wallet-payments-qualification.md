# Public wallet payment qualification

## Current fingerprint, Ironwood and recovery followthrough

SDK/harness `29aee5560f17c9c36ab3957d5519cb294fc47223` passed the existing Node filesystem and Firefox OPFS matrix. Real native UFVK reimport with no account index exercises fingerprint-based external attachment and signing through the wallet components. The public factory workflow additionally spends native-generated Ironwood funds, replays a same-height empty-block fork, and discovers two finalized operations in one database. Prior separately qualified draft/recovery evidence remains retained. Funding remains a synthetic native fixture; this is not live-chain qualification.

The fork exposed an owned-binding bug: completion required a checkpoint at an empty tip even though upstream need not create one. Native `bb74dace75d6f1ca3c43108cf46390db9469b5e4` verifies each tree root at the target block's recorded tree size using the existing upstream root API. Block identity and coverage guards remain enforced. The named native regression and a copied retained-database check passed, including rejection of incorrect frontiers/hashes.

The unchanged producer completed with receipt `635f8a8163fbd94c4c7ff0a2d6a47b5b3971b070b18475f7bfbe7f4428e4cdb7`. Package `package-signer-fingerprint-02` has manifest `8388ccc38b861ef5da60eaa7ced46809afaea826dd8821769a117bc780a7f677`; its source/artifact closure passed independent review.

- Node original process `95998` exited zero: 288 runtime asset requests, ten proving-parameter requests, and worker-dispatch crash recovery. Log `/tmp/final-wallet-gaps-node-05.log`, SHA-256 `731d191116fa8eff4e3d01090c2f019497655aa201b7d67c28b9d2100a7ad61a`.
- Firefox original process `5957` exited zero: 45 workers destroyed, all workflow flags true, no unexpected requests, and complete cleanup. Receipt `/home/jack/zcash-final-wallet-gaps-firefox-05-logs/firefox-3EjUSL.json`, SHA-256 `1539a37abc3c6bc70f864780d2d433782ac2b75059765ca96adb9421fc1a6295`.
- The existing native Firefox runner separately exited zero (`72814`), including OPFS commit-sync rollback. Receipt `/home/jack/zakura-account-compose-logs/fixes/r1/firefox-result-1789337027693.json`, SHA-256 `6186c270acbaa3d47d1ebd8385723811982770a6cba552edc3b56f083c4c6831`.

Earlier failed runs remain preserved: obsolete fingerprint/count assertions, missing bounded fixture history, and the empty-tip defect were corrected before the final pass. This does not close threaded support (#107), deferred finalized-transparent PCZT support (#102), or the remaining qualification work tracked in #115/#116.

## Historical initial payment qualification

The synthetic baseline wallet workflow passed on Node filesystem storage and Firefox OPFS on 2026-09-13. This qualifies the payment integration described below; it does not complete the frozen SDK scope.

Both hosts exercised the public wallet factory, mnemonic account and signer attachment, sync, balance and history, transfer, shielding, and the native two-step TEX route. Assertions cover committed cancellation without dispatch, ordered submission, exact-byte retries after signer disposal, operation discovery after reopen without application-saved IDs, reopening without proving parameters, pre-dispatch cancellation, and cleanup. PCZT checks separately exercise native proving and finalization with Sapling and Ironwood outputs.

The funding transaction and database are generated by native fixtures. Endpoints are synthetic local gRPC in Node and gRPC-Web in Firefox. These runs do not establish live-chain mining, the full automatic recovery matrix, threaded runtime support, or the remaining contract gaps #97 and #98. Already-finalized external transparent PCZT inputs are explicitly unsupported and deferred in #102. Deferred issues #10–12 remain unchanged.

## Bound sources and artifacts

- SDK and harness revision: `b25ea5c92b63abb7d91d86ce6aca0af8eda95d85`.
- Native source revision: `57df980e72e8dcb114b10edecb57ab122bac12b9`.
- Native build receipt SHA-256: `9f067ad39a0c1a35c72598dbf22cfa4d02971acc1c3592353a5ecd0724f6c79b`.
- Runtime manifest SHA-256: `3aec2cfb660c488c7309fd3711426108d879312d78034f3cd9ab59821b6fecad`.
- WASM SHA-256: `940d807cc35ecefffbc9c3db8130e51c6e758e28b7b44ed52468fd25539a13eb`.

Independent review checked the native source archive, all producer command results, package assets, compiler and worker inputs, and host evidence. All 20 native producer commands exited zero. The generated module retains a nonshared memory maximum of 4,096 pages. Its clock uses the pinned `time` dependency's existing WASM feature; the two generated Date imports were individually reviewed.

## Actual host results

Node's original process exited zero. Its assertions include 192 verified runtime asset requests, eight proving-parameter callbacks, no unexpected requests, and worker-resource cleanup. The full log is retained locally at `/tmp/wallet-payments-runtime-node-03.log`, SHA-256 `67879f6a317c7dbf1a016f60a164919ac722965e70f5ae8d3af328d0bdc61d8b`.

Firefox's original process exited zero. Its result reports all public payment checks passed, 31 worker destructions, 192 closed RPC responses, no unexpected requests, and complete browser, driver and server cleanup. The workflow completed in 268,948 ms. The receipt is retained at `/home/jack/zcash-wallet-payments-firefox-02-logs/firefox-QNXZzs.json`, SHA-256 `04cd5733e052c77254e31834fc546aba061c03ebef05c76c7048a07d5ecf7c37`; its 81 saved asset hashes were checked against the emitted modules and committed harness sources. TLS verification stayed enabled.

The reusable runners are `tests/wallet/runtime-loader-node.mjs` and `tests/wallet/host-browser.mjs`, sharing `tests/wallet/public-wallet-checks.mjs`. The accepted local inputs are `/home/jack/zakura-wallet-runtime-scratch/package-wallet-payments-03`, `/home/jack/zakura-account-compose-scratch/fixes/r1/wallet-payments-build-03`, and canonical parameters in `/home/jack/zakura-native-pczt-prove-scratch`. These paths describe retained qualification inputs, not a distributable package installation contract.

Earlier failing runs remain preserved. They exposed a WASM clock trap during finalization; enabling the existing clock feature corrected it. A retained proved transaction then passed finalization, exact retry without parameters, and fresh-worker reopen before the complete host reruns above.

## Online startup recovery followthrough

Harness revision `dd6e10f6755d250df284229104108202e803c31e` adds real filesystem and OPFS checks using the same native build and runtime manifest above. Opening discovers and observes a canceled finalized operation without granting its first dispatch. Subsequent opens observe without retry by default, retry exact stored transactions in dependency order under explicit policy, and preserve the stricter automatic retry budget when a later open requests a larger budget. Each transfer, shielding and TEX wallet contains one operation; this does not establish a multi-operation crash matrix.

Both original host processes exited zero. Node enforced 246 verified runtime asset requests, eight proving callbacks, and cleanup; its log `/tmp/wallet-startup-recovery-node-01.log` has SHA-256 `f317d9aa268bcea97c77dfe22700696ae087d255c4b6bbe7db172f9f6f44c4a1`. Firefox completed in 293,754 ms within the unchanged deadline, with 40 worker destructions, no unexpected requests, and complete browser/driver/server cleanup. Its receipt `/home/jack/zcash-wallet-startup-recovery-firefox-01-logs/firefox-E3YdCt.json` has SHA-256 `2d4e39dbefb7d33893ac43f839b1c8d79e47c0cf6d7d5b73f2e4e698eae34b71`. TLS verification remained enabled. The SDK build and all 14 focused payment tests also passed.

## Multiple operations and Node worker interruption

The shared workflow at `12c4aca3f8542cc2c9c1c88523cca3c5b1a07ac7` retains a real shielding draft and finalized transfer in the same native database. Public reopening reconciles both, while only the finalized payment is an observation candidate. Pagination discovers their IDs; the draft stays unfinalized and cannot broadcast or wait. Native input selection confirms that the draft reserves transparent funds and the transfer spends separate Sapling funds. This covers two operations in one account, not multiple finalized candidates or a large-inventory fairness matrix.

Node's original two-operation run exited zero; `/tmp/all-operations-node-01.log` has SHA-256 `ab64416a627656a370a052ccb08e0de7644964f9b2cd878d3bae1c8ef32540c2`. A further Node-only check at `ecdd9664ec3c3caf3f09baaa56e7fe1bdc51d0f3` terminates the actual wallet worker after the server receives a retry's bytes, before acknowledgment and durable attempt completion. Public reopen records that interrupted attempt as unknown; explicit retry sends identical bytes and completes. It uses no signer or proving parameters for recovery and preserves the separate draft.

The original crash-check process exited zero. Its log `/tmp/worker-dispatch-crash-node-01.log` has SHA-256 `24cf16c0417f17e2296854d0e0626e9cc092ab3a1589342153f03850e3e8044c`; assertions enforce 258 runtime asset requests, eight proving callbacks for the preceding workflows, and complete executable-directory cleanup. Independent review compared all 103 emitted production files with the prior accepted build and verified the unchanged package03 assets. This is Node worker death during dispatch, not whole-process or browser crash qualification.

The first two-operation Firefox run failed at its 300-second page deadline; its retained receipt is `/home/jack/zcash-all-operations-firefox-01-logs/firefox-S87ZdT.json`. Cleanup completed, with no unexpected request or reported application error. Commit `ffac5a1e1a97215457270b5513c134e2b21a2513` extends only the full-proving harness page/outer allowances to 420/450 seconds. Production runtime timeouts and smaller-test budgets are unchanged. This failed run is not accepted evidence of browser completion.

The second Firefox attempt (`firefox-WOHqv8.json` in `/home/jack/zcash-all-operations-firefox-02-logs`) exposed a real timer-initialization race: if the monotonic clock advanced while a one-millisecond pause was being set up, its callback could reference the abort handler before initialization. Commit `ec08f5e20d17cc4c19aed880ed13ead1ca05135c` schedules the first timer check asynchronously. The deterministic regression fails with the old implementation (`/tmp/wallet-recovery-timer-red.log`) and passes with the fix; all 15 payment tests and the SDK build pass.

Both original final host runs against that fix exited zero. Node's `/tmp/wallet-recovery-final-node-01.log` has SHA-256 `ed009b3d63115f12181398668bae6322b099551605e97894384e8f5983e5d466` and confirms both multiple-operation recovery and real worker-dispatch interruption, with 258 runtime asset requests and cleanup. Firefox's `/home/jack/zcash-wallet-recovery-final-firefox-01-logs/firefox-jvhAcA.json` has SHA-256 `28adabe191a680223607e577de16e4d897d3a736a5c651b172ed71dab7992137`; it completed in 288,169 ms with multiple-operation recovery, 40 worker destructions, no unexpected requests and complete browser/driver/server cleanup. Firefox does not run the Node-only worker-death check. Both use the unchanged accepted native build and runtime manifest, with TLS verification enabled.

## Browser worker interruption and fresh-page recovery

The opt-in existing Firefox harness at `b7cd7325b4104ef335ee2200c8a4ffc8e573bed9` stages a closed native database containing one finalized transfer and one draft. It holds an actual gRPC-Web submission response, confirms the broadcast is unsettled and no attempt-finish command has been dispatched, terminates the captured native Worker, and navigates to a fresh page. Public database discovery then observes the interrupted attempt as unknown; explicit retry sends identical bytes and preserves the draft. This qualifies worker termination plus page replacement, not in-page silent-worker detection or whole-browser/process failure.

Original process 39433 exited zero in 6.090 seconds. Receipt `/home/jack/zcash-browser-dispatch-crash-01-logs/firefox-FYyS8T.json` has SHA-256 `a65f19725a3dcf7d1938e47b0d40b7c5c3c180c6e5d698c821c3d3ae4951a31f`. All 80 saved asset hashes were verified, including 50 emitted modules, 12 harness files and six unchanged package03 runtime files. The closed source database remained unchanged (`c1864b3cf88b7a1018cd72e9a44ac1f6b04fef0cdec01b83d2f26aa8ab59f373`). Both actual submission responses and all 16 RPCs closed; the reopened worker closed normally, and browser/driver/server cleanup completed without errors or unexpected requests. No proving work or native rebuild was performed. TLS verification remained enabled.
