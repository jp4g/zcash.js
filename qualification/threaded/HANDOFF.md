# Scanner integration seam

The generated threaded artifact is a synthetic runtime bootstrap slice, **not F2 scanner completion**. No compact scanner fixture was created here. Integrate the genuine fixture maintained by its assigned worker using the following contract.

1. Preserve the Common 1.0 dependency package records in `Cargo.lock`. Add the fixture adapter as an owner export in `src/lib.rs` and an explicit operation in `worker.mjs`; keep fixture generation and algorithms in the assigned scanner qualification. Use the exact same fixture bytes/digests as the serial comparison.
2. `Domain.start()` is the only entry to bootstrap. The owner initializes the generated module and `owner_prepare(2)`. The supervisor clones the exact compiled module and shared `WebAssembly.Memory` into two direct compute workers. Every compute worker runs real generated start/TLS/stack initialization, reports loaded, then blocks in `worker_enter(index)`. The supervisor tells the owner to `owner_build()` only when all workers loaded. Rayon publishes thread builders through std synchronization and `build_global()` waits until primed. Only then does the owner report ready and the supervisor admit operations.
3. The Rust owner calls `assert_ready()` before the new scanner export. SQL, Common BLS, fixture state and mutation/commit ownership remain exclusively in that worker. Rayon child threads may run scanner compute, never direct SQLite calls. The existing `parallel_evidence()` can demonstrate pool execution before and after scanner completion, but is not itself scanner evidence.
4. `Domain.call(operation,args)` sends one explicit operation to the owner. Large fixture arrays need deliberate copy/transfer policy; this synthetic scalar adapter currently uses structured clone. Do not introduce a public API or production scanner/storage patch as part of this seam.
5. Record Node and Firefox bounded completion, all supported pools, multiple batches, and equality of notes/nullifiers/trees/checkpoints/spentness. The immutable fallback is a separate generated non-shared artifact. This branch proves fresh baseline selection only with ephemeral SQL/BLS, not serial scanner parity.
6. Any startup error/timeout destroys the entire partially initialized domain before a fresh baseline qualification. Any fault after readiness invalidates the domain; no fallback/mutation replay. An external supervisor deadline remains live while the Rust owner is blocked. Node termination is awaited; Firefox teardown is independently checked through BiDi realm destruction and empty live-worker inventory.

Artifact packaging: `source qualification/threaded/env.sh; python3 qualification/threaded/build.py`, then `python3 qualification/threaded/package.py NEW_BUNDLE CAPTURED_BUILD`. Update the pinned manifest in `evidence.json` only for the new reviewed bundle. Never edit generated glue or reuse shared memory after a failed pool. The existing baseline build at `/home/jack/zcash-generated-runtime-scratch/build-1789135187453484355` stays read-only.

Full F1 wallet/prover invoked-export closure, F2 scanner equivalence, durable VFS, bundler/deployment/CSP matrix, and independent HIGH review are not claimed by this bootstrap qualification. No push/merge.
